name: macOS VNC BlackScreen Fix

on:
  workflow_dispatch:

jobs:
  macos-session:
    runs-on: macos-latest
    timeout-minutes: 60

    env:
      # ====================================================
      # ضع كود Base64 المشفر هنا
      # ====================================================
      aa1: "dHNrZXktYXV0aC1rc2JSdEZKZDJ0MTFDTlRSTC1yWlk4SjF2SGdZZ3Q4N0NQanFFQ1lnM0ZFMTFGTU1XQlI="
      
      # بيانات الدخول
      NEW_USER: "adminuser"
      NEW_PASS: "MacPass1234!"

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # ====================================================
      # 1. تشغيل Tailscale
      # ====================================================
      - name: Install & Connect Tailscale
        run: |
          brew install tailscale
          sudo brew services start tailscale
          sleep 5
          
          ENCODED=$aa1
          if [ -z "$ENCODED" ]; then echo "Error: Key is empty"; exit 1; fi
          REAL_KEY=$(echo "$ENCODED" | base64 -d | xargs)
          
          # الاتصال
          sudo tailscale up --authkey=$REAL_KEY --hostname="Mac-Fix-Screen" --ssh
          echo "✅ Tailscale IP: $(tailscale ip -4)"

      # ====================================================
      # 2. إنشاء المستخدم وتفعيل VNC (مع حل الشاشة السوداء)
      # ====================================================
      - name: Fix Black Screen & Setup User
        run: |
          echo "Creating new admin user: $NEW_USER ..."
          sudo sysadminctl -addUser $NEW_USER -fullName "Admin User" -password "$NEW_PASS" -admin
          
          # 1. منع الكمبيوتر من النوم
          sudo systemsetup -setcomputersleep Never
          sudo systemsetup -setdisplaysleep Never
          
          # 2. تفعيل VNC
          echo "Enabling VNC..."
          sudo /System/Library/CoreServices/RemoteManagement/ARDAgent.app/Contents/Resources/kickstart \
          -activate -configure -access -on \
          -clientopts -setvnclegacy -vnclegacy yes \
          -clientopts -setvncpw -vncpw "$NEW_PASS" \
          -restart -agent -privs -all
          
          # 3. محاولة تفعيل الدخول التلقائي (لحل الشاشة السوداء)
          echo "Configuring Auto Login..."
          sudo defaults write /Library/Preferences/com.apple.loginwindow autoLoginUser -string "$NEW_USER"
          
          # 4. الخطوة الأهم: إعادة تشغيل واجهة الرسوميات لإجبار الشاشة على العمل
          echo "Restarting WindowServer to force display refresh..."
          sudo killall -HUP WindowServer || true
          
          echo "✅ Configuration Complete!"

      # ====================================================
      # 3. إبقاء الجلسة
      # ====================================================
      - name: Keep Session Alive
        run: |
          IP=$(tailscale ip -4)
          echo "==================================================="
          echo "🚀 CONNECT NOW!"
          echo "IP: $IP"
          echo "Username: $NEW_USER"
          echo "Password: $NEW_PASS"
          echo "==================================================="
          echo "💡 TRICK: If screen is still black, type the password blindly and hit Enter!"
          
          while true; do sleep 60; done
